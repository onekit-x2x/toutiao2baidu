<switch 
class="onekit-switch {{onekitClass}}"
style="{{onekitStyle}}"
id="{{onekitId}}"

checked="{{checked}}"
type="{{type}}"
color="{{color}}"
disabled="{{disabled}}"

bindchange="switch_change"
>
<slot/>
</switch>